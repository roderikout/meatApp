// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: 'AIzaSyCw0oc-p0_IQSCKvlseo6r55sw49HDyrCQ',
    authDomain: 'meatappv1.firebaseapp.com',
    databaseURL: 'https://meatappv1.firebaseio.com',
    projectId: 'meatappv1',
    storageBucket: 'meatappv1.appspot.com',
    messagingSenderId: '214502559450'
  }
};

//This shouldn't be on Github

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
